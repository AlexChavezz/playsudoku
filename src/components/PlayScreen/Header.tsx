export const Header = () => {
    return (
        <header>
            <span 
                className="material-symbols-outlined palette"
            >
                palette
            </span>
        </header>
    );
}